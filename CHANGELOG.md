# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与语义化版本。

## [0.7.1] - 2026-09-11

### 修复(分叉/侧边对话的日志把父会话历史又算了一遍)

- **现象**(用户发现):统计页里同一次回复出现 2–3 行**完全相同**的记录(同时间、同用量、同金额),
  当日合计因此虚高——截图里 ¥44.96 的那一天,真实只有 ¥18 左右。
- **原因**:DSH 的 fork / 侧边对话(side thread)会把**父会话的事件整段复制**进自己的日志。
  实测:一个侧边对话复制了 712 条 `assistant/message` + 23 个 `turn/start`,
  而折叠逻辑是按「整份日志」统计的,于是父会话的用量被重复计费;
  父会话自己的日志本来就已经算过一次,所以这是纯粹的多算。
- **修法**:读 `session` 头部的 `isSeeded`;为真时先定位接缝——
  `session/end-seed` 事件里 `data.inherited === true` 的那条(DSH 在嫁接处写下的分界),
  只统计接缝**之后**的自身事件。若声明了 seeded 却找不到接缝,则**整份不计**
  (父会话的日志会算一次;宁可少算也不错算,与「绝不编数字」同一条原则)。
- **顺带修好**:`session` 头部字段在 DSH 0.1.5 里写在事件的**顶层**(不在 `data` 里),
  旧代码按 `data.*` 读永远读不到 → `delegationDepth` 一直是 0,「子代理」标签从未生效。
  现在两种写法都读,子代理/侧边对话自身的回复会正确带上「子代理」标签。
- **实测**(同一份日志,只切换折叠实现):

  | | 今日(09-11) | 全部 |
  |---|---|---|
  | 修复前 | ¥45.25 / $6.79(62 条回复) | ¥64.13 / $9.62(107 条 + 4 压缩) |
  | 修复后 | **¥18.19 / $2.73(24 条回复)** | **¥35.18 / $5.28(56 条 + 2 压缩)** |

  也就是说,此前当日金额里约 **60% 是重复计数**。
- `verify-balance` 多打一行 `seeded logs : N fork(s), M inherited events skipped`,
  让「跳过了多少继承事件」可见,而不是静默丢弃。
- 新增 5 项单测:接缝前的继承事件不计、自身回合照计、无接缝则整份不计、
  非 seeded 日志不受影响、旧式 `data` 嵌套头部仍可读。
- 说明:此前的三次余额对账**结论不受影响**——被跳过的副本都归属到更早的时刻,
  两个较新的分叉又是在最后一个对账点之后才建立的,所以窗口差值没有被污染。

## [0.7.0] - 2026-09-11

### 变更(改名 dsh-cost-stats + 安装方式改成最佳实践)

- **包名 `dsh-session-cost` → `dsh-cost-stats`**。原因很实际:npm 上 `dsh-session-cost`
  与 `dsh-turn-cost` 都已被另一位作者占用(功能也是「估算会话费用」),原名字既发不上去,
  又会让别人照着 README 装到别人的插件。仓库名、`dsh.plugin.json`、代码里的模块标识
  (`cordis.patch.yml` 的 loader row id、宿主路由 `/cost-stats/usage`、locale 命名空间
  `cost-stats`、样式类前缀 `dsh-cost-stats__*`)一并改名;`src/host/cost-stats-index.ts`
  是随之重命名的文件。
- **安装章节重写**,参照生态里成熟插件(DSH-better-sidebar)的写法:
  前置要求 → 支持的 DSH 版本 → **四条安装路径**(本机 tarball / 源码 `link:` / npm(发布后) /
  把提示词交给 DSH 自己装)→ 装完必须做的一步(client 改动硬刷新、**host 改动必须重启**)→
  「更新」「卸载」「常见问题」三个可折叠小节。常见问题表里每一条都是本项目真实踩过的坑。
- 新增 **`dsh.plugin.json`**(插件市场清单:id / version / main / `engines.dsh` / client.main),
  与生态里其他插件的清单字段保持一致。
- README 顶部加徽章(CI / stars / DSH 版本 / License / 无内核改动 / 中英双语)。

### 隐私(公开前清理)

- 去掉 `package.json` `author` 里的邮箱,只留 GitHub 用户名。
- README/CHANGELOG 里的对账记录**不再出现账户余额绝对值与累计花费绝对值**,只保留
  「余额下降 / 复算上升」的差值——对账结论不变,但不再暴露个人账单数字。
- 效果截图的「会话」列做了模糊处理(原来能看清真实会话标题)。

## [0.6.1] - 2026-09-11

### 文档(加了效果截图)与一处文案修正

- README 新增 **📸 效果** 一节,放两张本机真实截图(不是示意图):
  - `docs/images/turn-cost-chip.png` —— 消息旁的 `≈¥0.100` 胶囊 + 分档明细面板
    (计费模型 / 请求模型 / 计费时段 / 缓存命中 99.9% / 三档「token × 单价 = 金额」/ ¥ 与 $ 合计);
  - `docs/images/cost-stats-page.png` —— 费用统计页:合计卡片、带「压缩」标签的明细、分页;
  - 两张图也加进 `package.json` 的 `files`,这样 npm 页面上的 README 不会显示裂图。
- **修文案**:胶囊面板底部原写「子代理、后台会话与**压缩**请求不计入」——0.6.0 起压缩已计入,
  这句话与实现矛盾。现在改为「子代理与后台会话的请求不计入;压缩是独立计费项,见费用统计页」。
  (README 里两张截图仍是修正前拍的,那句话以实际界面为准。)
- README 已知边界同步:不再把「压缩」列为未折叠项。

## [0.6.0] - 2026-09-11

### 新增(把「压缩」算进来——余额对账发现的真实缺口)

- 背景:一个对账窗口里,余额降 $0.78 而复算只升 $0.575。差额 $0.205 已定位到
  **本会话触发的一次上下文压缩**(`compaction/summary`);压缩是独立模型调用,
  既不属于任何回复,官方「用量」胶囊也看不见它。
- **`compaction/summary` 现在会被折叠成独立计费项**:`host/turn-fold.ts` 新增
  `FoldedCompaction`(`at` / `provider` / `model` / 分档 token),宿主索引为它生成一行
  (`turn: 0` + `compaction: true`),统计页显示为带**「压缩」标签**的一行。
- 合计口径:压缩的**钱、用量、会话数都计入**,但**不计入「回复」数**——
  指标行显示 `回复 16 · 压缩 1`,不会让人误读成 17 次回复。
- 胶囊兜底**不会**把压缩行当成回复:`usage-store.lookup` 明确跳过 `compaction: true`
  (`sessionId + turn` 取行,而压缩的 turn 是 0),新增单测断言。
- `verify-balance.mjs` 同步计入压缩并打印数量,独立复算与宿主路由口径一致。
- 结果(用同一套脚本复现):

  | 区间 | 余额变化 | 复算变化 | |
  |---|---|---|---|
  | 11:07 → 12:20 | −$0.25 | +$0.249 | ✓ |
  | 12:20 → 15:02 | −$0.78 | +$0.77 | ✓ 1 美分内 |

- 新增 7 项单测(压缩折叠 4 项:独立计费项 / 自己带路由 / 非法用量丢弃 / 无路由不猜;
  合计 2 项:压缩单独计数、压缩不算子代理回复;缓存 1 项:兜底永不取压缩行),共 56 项。
- 文档:README 对账表重写为「压缩前后的真实差额」,并把压缩列入已知边界;架构文档同步。

## [0.5.3] - 2026-09-11

### 变更(按反馈:合计卡片同一行)

- 合计卡片的「回复 / 会话 / 用量」原来垂直居中,与左边的金额**不在一条线上**;
  现在把金额与指标放进同一行并用 `align-items: baseline`(基线对齐),两者落在**同一条横线上**。

### 验证(与 API 余额对账,发现唯一系统性缺口)

- 新增对账点:相对上一个对账点,余额降 $0.78,而复算只升 $0.575,**差 $0.205**。
- 差额已定位:`compaction/summary`(**上下文压缩**,本会话触发一次,
  707,231 未缓存输入 + 22,016 缓存读取 + 4,896 输出,按当时高峰价 **¥1.45 / $0.218**)。
  压缩是独立模型调用,不属于任何回复,官方胶囊与插件的逐回合折叠**都看不见它**。
  加回后 $0.793 vs 余额 $0.78,残差 $0.013(余额两位小数舍入 ±$0.01 + 标题生成调用)。
- 顺带核实:全部日志中 `cacheWriteTokens > 0` 的样本为 **0**,所以价目表没有「缓存写入」档这件事在实测中不产生误差。
- README「与账户余额核对」补上对账表与该发现。

## [0.5.2] - 2026-09-11

### 变更(按反馈:每页 15 条、合计卡片对齐)

- **每页 15 条**(原 20 条):分页文案里的「每页 N 条」自动跟随,不用改文案。
- **合计卡片不再「上轻下重」**:美元从第二行挪到人民币旁边(同行、浅色、与 ¥ 基线对齐),
  右侧「回复 / 会话 / 用量」由**底部对齐**改为**垂直居中** —— 之前它俩都贴在卡片下沿,
  现在整块内容居中,卡片也更矮。

## [0.5.1] - 2026-09-11

### 变更(按反馈:统计页默认与排版)

- **默认打开就是「今天」**:日期筛选初始值 = 本地当天,列表因此默认只显示当天的回复;
  想看别的日子点日期、想看整月点月份、想看全部点「清除筛选」。
- **列表统一左对齐**:表头与四个列(时间 / 会话 / 用量 / 费用)全部左对齐,去掉了原来的右对齐数值列。
- **行变矮、垂直居中**:费用列不再把 $ 换到第二行(`¥1.09 $0.164` 同行显示,$ 为浅色),
  单元格改为垂直居中、内边距 7px→6px —— 之前每行下半截空着,就是被那第二行撑高的。
- **分页**:每页 20 条,底部给出 `第 x / y 页 · 共 n 条 · 每页 20 条` 与「上一页 / 下一页」,
  到头的按钮自动禁用;换筛选自动回到第 1 页,选择变少时页码自动夹回有效范围。
  分页算术抽成纯函数 `paginate`(`stats-model.ts`),新增 5 项单测(切页、越界夹取、
  非正数/小数/NaN、空选择、每页 0 条)。
- 空态文案补上出口提示(换个日期或「清除筛选」)。
- 冒烟脚本修正 React 桩:`useState` 现在支持惰性初始值(函数形式),并新增「页面默认选中今天」断言。

## [0.5.0] - 2026-09-11

### 修复(核心用量被扣留时,回合费用不再整块消失)

- 起因:官方「用量」药丸有时不显示。查证结论——**不是插件或兼容性问题**:
  DSH 核心的回合用量折算是「全有或全无」,只要该回合里有一次重试请求没回报用量
  (例:请求发往 `api.deepseek.com` 失败后重试成功),整个回合的 `tokenUsage` 就被判为不可信,
  官方药丸与本插件(读同一个字段)都会一起消失。
- 选择**不动官方代码**,只补插件侧:**精确路径取不到数时,改用插件自己的宿主侧日志重算**。
  - 新增 `src/client/usage-store.ts`:一份共享的、30 秒有效期的宿主数据缓存,同刻多个药丸只发一次请求;
  - 药丸顺序不变:先试官方数据(数字与官方面板一致);取不到时才回退,回退值在标题与浮层里
    明确标注「按日志重算」,绝不冒充官方口径;
  - 回退按「会话 + 回合号」精确取行,不会串到别的会话;仍然只在你点开浮层时才有内容。
- 新增 9 项 `UsageStore` 单测(并发合并、TTL、强制刷新、失败保留旧数据、按会话+回合查找、订阅退订);
  冒烟脚本扩到 21 项,新增「首屏不显示 → 回退后可显示 → 标注为重算 → 不借用其他会话」四项。
- 精度说明:回退用的是与统计页同一套日志折算,子代理/后台/压缩请求照旧不计入。

## [0.4.0] - 2026-09-11

### 变更(按反馈)

- **查询控件改成日历选择器**,不再是「按日 / 按月」选项卡:
  - **选择日期** → 一个周一开头的日历(6×7 网格、可翻月、「今天」有标记),点某一天即筛选;
  - **选择月份** → 一个 12 个月 + 可翻年的月份网格,点某个月即筛选;
  - 两者互斥(选了日期就清掉月份,反之亦然),另有「清除筛选」与「刷新」;
  - 有数据的日期/月份在网格里加点并加重显示,没数据的日子也能点(显示空态)。
- **只保留一份明细列表**:移除了「按日 / 按月」下的日期/月份汇总表 —— 页面现在只有
  逐次回复明细(时间 / 会话 / 用量 / 费用),不会再出现两套口径。
- **合计卡片标注当前范围**:`费用合计 · 2026-09-11` / `· 2026-09` / `· 全部时间`,跟随选择变化。
- 新增 `src/client/calendar.ts`(纯日历数学:周一为首的 6×7 网格、跨年月份平移、`YYYY-MM` 解析)
  与 `src/client/Pickers.tsx`(两个选择器 + 共享锚定浮层),共 11 项新单测。
- 清理:统计模型只保留分组键与合计(移除分桶表相关代码),样式表同步删除未使用的类。

## [0.3.0] - 2026-09-11

### 变更(按反馈)

- **统计页改成"每一次回复"的明细列表**(原来按会话汇总):表格列出每条回复的 时间 / 会话 / 用量 / 费用;
  会话带 `子代理` / `无价目` 标记,悬停可见 `会话 · #回合 · 模型`。
- **合计跟随查询**:顶部合计卡片按 `按日` / `按月` / `全部回复` 的当前选择统计(日/月分桶表点行即筛选)。
- **移除原来的说明块**(按会话汇总、子代理说明、未纳入计数等):页面只保留标题、查询、合计、两张表与状态提示。
- **宿主半边不再是空插件**:新增 `GET /cost-stats/usage`,由宿主读取**全部会话日志**折叠出逐次回复的
  provider / model / 分档 token / 时间,再用同一套价目表计价;客户端只做分组与合计——日/月统计与逐条明细用的是同一批数字。
- **逐次精确**:模型取该次回复的 `message.source`(缺失时回退到最近一条 `model/selection`),
  高峰/空闲按该回合自身的起止时刻判定——比 v0.2 的"按会话最近模型 / 最后活动"更准。
- 新增 `pnpm run verify:balance`:独立诊断脚本,绕过宿主直接读 `$DSH_HOME/sessions/**` 复算,
  并给出指定时刻的累计值(用于和 API 余额的差值对账)。
- 测试 31 项(新增 7 项事件折叠、重写 11 项统计模型),冒烟 17 项(含两个条目的抛错隔离、统计页发起宿主请求)。

### 验证记录

用 `verify:balance` 独立复算全部会话的累计费用,并与 API 余额的两个读数对齐:

| 时刻 | 余额 | 独立复算累计 | Δ |
|---|---|---|---|
| 11:07 | (对账点) | 复算累计 A | — |
| 12:20 | (对账点) | 复算累计 B | **+$0.249** |

余额同区间下降 **$0.25**,与复算差值 **$0.249** 一致(1 美分以内)。

## [0.2.0] - 2026-09-11

### 新增

- **设置页「费用统计」**(注册进 `settings.section`,排在「侧边卡片」之后):按会话列出用量与费用,
  顶部可在 **按日 / 按月 / 全部会话** 之间切换,并有跟随查询的**费用合计**卡片(¥ 与 $、会话数、用量、子代理数、未计价数)。
- 日/月分桶表(会话数 / 用量 / 费用),点某一行即可把下方会话明细筛到该桶,再点一次取消。
- 会话明细行:时间、标题、`子代理` / `进行中` / `无价目` 标记、用量、费用(¥ 主 / $ 次)。
- 数据来源改用**宿主会话投影**(`tokenUsage` 累计分档 + `modelSelection` 模型归属 + `updatedAt`),
  因此**不需要激活任何冷会话**即可汇总整个会话列表;未持有用量投影的会话会被明确计数为"未纳入"。
- 新增 11 项会话聚合单测(日/月键、分桶、筛选、合计、子代理、无价目、模型回退);冒烟脚本扩到 19 项
  (含**统计页渲染抛错同样必须被隔离**、统计页真实算价与标记)。

### 边界(如实记录)

- 统计页是**会话**级粒度:金额按该会话**最近使用**的模型计价,中途换过模型的会话为近似值;
  高峰/空闲按会话**最后活动时刻**归类。逐回合的精确视图仍是聊天里的费用胶囊。
- 子代理会话单独成行,不并入父会话(由宿主投影的 `origin === 'subagent'` 判定)。

## [0.1.0] - 2026-09-10

### 新增

- 助手消息尾行(`conversation.chat.assistant-actions` 插槽)显示本回合费用胶囊(`≈¥0.709`)。
- 点击展开明细面板:计费模型、计费时段(高峰/空闲)、缓存命中率、未缓存输入/缓存读取/输出三项
  「token × 单价 = 金额」、本回合合计(¥ 与 $)、本次会话已加载回合累计。
- 官方价目表(人民币 + 美元两套公布值):DeepSeek-V4.1-Flash 与 DeepSeek-V4-Pro-0813。
- 模型名计费归属:`deepseek-flash` 为规范名;`deepseek-v4-flash` / `deepseek-v4-flash-vision-exp`
  为旧名,按 Flash 计费;`deepseek-v4-pro` 自北京时间 2026-09-14 12:00 起改按 Flash 计费。
- 高峰时段判定:北京时间周一至周五 09:00–12:00、14:00–18:00,跨时段回合在面板中标注。
- 无法计价(无路由归属 / 无公示价)时**不渲染胶囊**,不显示 0 或猜测值。
- 中英文字典(命名空间 `cost-stats`),随 DSH 界面语言切换。
- 17 项计费单测 + 无浏览器的产物契约冒烟脚本 + `pnpm run verify` 一键门禁。

### 边界(如实记录)

- 只统计已加载到聊天窗口的回合;更早的历史需滚动加载后才计入。
- 子代理、后台会话与压缩请求不计入(属于各自的会话/账目)。
- 回合内混用多个计费模型时按最高价估算上界(DSH 的回合用量是聚合值,无法按模型拆分)。
